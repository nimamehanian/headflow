.app
  display flex
  justify-content center
  align-items center
  margin-top 76px
  margin-bottom 24px
  .editor
    width calc(100% - 48px)
    max-width 1200px
    background #fff
    padding 8px
    border-radius 4px
    {shadow}
